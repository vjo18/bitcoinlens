# 1) Haal remote wijzigingen binnen (houd jouw werk erbovenop)
git pull --rebase origin main

# 2) Maak (zo nodig) de workflow-map en het nieuwe workflow-bestand
mkdir -p .github/workflows

cat > .github/workflows/pages.yml <<'YAML'
name: Build & Deploy Jekyll (Chirpy)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - run: bundle install --without development

      - name: Build site
        run: JEKYLL_ENV=production bundle exec jekyll build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
YAML
